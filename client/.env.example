# Base URL of the Express API (no trailing slash). Required for “Sign in with Google” in dev:
# full-page navigations do not use the CRA proxy, so this must hit the server directly.
REACT_APP_API_URL=http://localhost:4000

REACT_APP_GOOGLE_CLIENT_ID=544320919470-dc5vnig55cc72aapc460hjes43ee98l3.apps.googleusercontent.com
